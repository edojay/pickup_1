class Caliculation {
  var numberOfStores;
  var numberOfGoods;
  var totalWeight;
  var price;

  void setNumberOfStores(var num) {
    this.numberOfStores = num;
  }

  void setNumberOfGoods(var num) {
    this.numberOfGoods = num;
  }

  void setTotalWeight(var num) {
    this.totalWeight = num;
  }

  void caliculatePrice() {
    /*if (numberOfStores < 3) price = 10 + price;
    else if (numberOfStores > 3 && numberOfStores < 10)
      price = 25 + price;
    else if (numberOfStores > 10) price = 100 + price;

    if (numberOfGoods > 0) price = 10 + price;

    if (totalWeight > 0) price = 10 + price;*/
  }
}
